- categories.each do |category|
  %div
    %h3= link_to category.name, category
    .description= category.description
    = image_tag category.cat_image.url(:thumb)

    %p
      Subcategories:
      = category.children.size
= link_to 'New Category', new_category_path
