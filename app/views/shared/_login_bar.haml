%ul.login-nav
  - if authenticated?
    %li
      = t(:'sessions.create.logged_in_as')
      - if current_user.visitor?
        = link_to my_profile_path do
          = current_user
          - if current_user.profile.present?
            = image_tag current_user.profile.avatar_url(:icon), :class => 'icon'

      - else
        = link_to my_profile_path do
          = current_user
          -#= image_tag current_user.provider.logo(:icon), :class => 'icon'

    - if authorized?
      %li= link_to t(:'site.nav.admin-panel'), admin_path
    %li= link_to t(:'sessions.destroy.title'), logout_url
  - else
    %li= link_to t(:'users.create.title'), new_user_url, :class => 'label label-success'
    %li= link_to t(:'sessions.create.title'), login_url
