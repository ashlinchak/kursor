%h1= t(:'admin.postings.posting_list.title')
%p= link_to t(:'admin.postings.posting_list.create'), new_admin_posting_path, :class => 'link-icon icon-add'
%hr/
%table.table.table-bordered.table-striped.table-condensed
  %thead
    %tr
      %th= t(:'admin.postings.posting_list.name')
      %th= t(:'admin.postings.posting_list.user')
      %th= t(:'admin.postings.posting_list.date')
      %th= t(:'admin.postings.posting_list.actions')
  %tbody
    - for posting in postings do
      %tr
        %td= link_to posting.title, admin_posting_path(posting)
        %td= posting.user
        %td= l posting.created_at, :format => :long
        %td
          = link_to t(:'admin.postings.posting_list.edit'), edit_admin_posting_path(posting), :class => 'link-icon icon-edit'
          %br
          = link_to t(:'admin.postings.posting_list.delete'), admin_posting_path(posting), :method => :delete, :confirm => 'Are you sure?', :class => 'link-icon icon-delete'
%hr
%p= link_to t(:'admin.postings.posting_list.create'), new_admin_posting_path, :class => 'link-icon icon-add'


