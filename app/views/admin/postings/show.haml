%h1= posting.title
%blockquote.excerpt= markdown posting.excerpt
.images
  - for image in posting.images do
    = link_to image.src_url(:medium) do
      = image_tag image.src_url(:thumb).to_s, :alt => image.title
.body= markdown posting.body

